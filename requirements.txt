requests>=2.28.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
markdownify>=1.2.0
# 1.45.0+ 필요 — max_completion_tokens(reasoning 모델 토큰 한도)와
# tool_calls 응답 형태를 쓴다. 그 이하 버전이 설치되면 OpenAI 폴백이
# 호출 시점에 죽는데, 폴백은 장애 때만 타므로 발견이 늦다.
openai>=1.45.0
# >=5: sync_overlap_precedents.py가 쓰는 Index.list(prefix=) 필요 — 3.0.0에는 없다
# >=8.0.0: build_bm25_corpus.py가 쓰는 fetch_by_metadata가 8.0.0 도입이다.
# 5.x가 설치되면 코퍼스 수집이 AttributeError로 죽는다(현재 실행은 9.1.0).
pinecone>=8.0.0
anthropic>=0.20.0
python-dotenv>=1.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
google-generativeai>=0.8.0
supabase>=2.0.0
PyJWT>=2.0.0
cohere>=5.0.0
networkx>=3.0
rank-bm25>=0.2.2
bcrypt>=4.0.0
nh3>=0.2.0
