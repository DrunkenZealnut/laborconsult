# Design: 근로복지공단 연락처와 홈페이지 정보 제공

> Plan 참조: `docs/01-plan/features/comwel-contacts.plan.md`

---

## 1. 변경 사항 요약

| # | 변경 | 파일 | 심각도 |
|---|------|------|--------|
| D-1 | 근로복지공단 데이터 모듈 신규 생성 | `app/core/comwel_offices.py` | Critical |
| D-2 | 시스템 프롬프트에 근로복지공단 안내 규칙 추가 | `app/core/pipeline.py` | Critical |
| D-3 | 컨텍스트 구성 시 근로복지공단 연락처 삽입 | `app/core/pipeline.py` | Major |
| D-4 | legal_hints 면책 문구에 근로복지공단 참조 추가 | `wage_calculator/legal_hints.py` | Low |

---

## 2. 상세 설계

### D-1. `app/core/comwel_offices.py` — 근로복지공단 데이터 모듈 (신규)

#### 1a. 데이터 구조

```python
from __future__ import annotations
from dataclasses import dataclass

@dataclass
class ComwelOffice:
    name: str                           # 지사명
    phone: str                          # 직통전화
    address: str                        # 주소
    jurisdiction: list[str]             # 관할 구·군·시
    parent: str | None = None           # 상위 지역본부명
```

#### 1b. 데이터 상수 (63개소)

지역별로 그룹화하여 `OFFICES` 리스트에 저장. 각 지사에 인덱스 부여.

```python
OFFICES: list[ComwelOffice] = [
    # === 서울 (0~8) ===
    # 0: 서울지역본부
    ComwelOffice(
        name="서울지역본부",
        phone="02-2230-9476",
        address="서울 중구 퇴계로 173, 남산스퀘어",
        jurisdiction=["중구", "종로구", "동대문구"],
    ),
    # 1
    ComwelOffice(
        name="서울강남지사",
        phone="02-3459-7153",
        address="서울 강남구 삼성2동 대종빌딩",
        jurisdiction=["강남구"],
        parent="서울지역본부",
    ),
    # ... (Plan §2.2 서울 참조: 2~8번)

    # === 인천·경기 (9~22) ===
    # 9: 경인지역본부
    ComwelOffice(
        name="경인지역본부",
        phone="032-451-9712",
        address="인천 남동구 미래로 16, 미추홀타워",
        jurisdiction=["남동구"],
    ),
    # ... (Plan §2.2 인천·경기 참조: 10~22번)

    # === 부산 (23~26) ===
    # === 대구·경북 (27~34) ===
    # === 광주·전라 (35~42) ===
    # === 대전·충청 (43~50) ===
    # === 울산·경남 (51~56) ===
    # === 강원 (57~61) ===
    # === 제주 (62) ===
]
```

**인덱스 할당 규칙**: 서울(0~8) → 인천·경기(9~22) → 부산(23~26) → 대구·경북(27~34) → 광주·전라(35~42) → 대전·충청(43~50) → 울산·경남(51~56) → 강원(57~61) → 제주(62)

#### 1c. 관할구역 매핑 dict

`REGION_MAP`: 시·군·구 이름 → `OFFICES` 인덱스 매핑. 광역시·도 이름은 대표 지사(지역본부)로 매핑.

```python
REGION_MAP: dict[str, int] = {
    # 서울 (광역 → 서울지역본부)
    "서울": 0,
    # 서울 구별 매핑
    "중구": 0, "종로구": 0, "종로": 0, "동대문구": 0, "동대문": 0,
    "강남구": 1, "강남": 1,
    "강동구": 2, "강동": 2, "광진구": 2, "광진": 2, "송파구": 2, "송파": 2,
    "마포구": 3, "마포": 3, "서대문구": 3, "서대문": 3,
    "용산구": 3, "용산": 3, "은평구": 3, "은평": 3,
    "영등포구": 4, "영등포": 4, "강서구": 4, "강서": 4, "양천구": 4, "양천": 4,
    "성북구": 5, "성북": 5, "도봉구": 5, "도봉": 5, "강북구": 5, "강북": 5,
    "중랑구": 5, "중랑": 5, "노원구": 5, "노원": 5,
    "관악구": 6, "관악": 6, "구로구": 6, "구로": 6,
    "금천구": 6, "금천": 6, "동작구": 6, "동작": 6,
    "서초구": 7, "서초": 7,
    "성동구": 8, "성동": 8,
    # 인천·경기
    "인천": 9,
    "의정부": 11, "동두천": 11, "양주": 11, "포천": 11, "연천": 11,
    "남양주": 12, "구리": 12,
    "고양": 13, "파주": 14, "수원": 15, "화성": 16, "용인": 17,
    "평택": 18, "부천": 19, "안양": 20, "과천": 20, "군포": 20, "의왕": 20,
    "안산": 21, "시흥": 21, "성남": 22, "하남": 22,
    "경기광주": 22,  # 경기 광주시 (광주광역시와 구분)
    # 부산
    "부산": 23,
    "해운대": 24,
    # 대구·경북
    "대구": 27,
    "포항": 30, "구미": 31, "경산": 32, "영주": 33, "안동": 34,
    # 광주·전라
    "광주": 35,
    "전주": 37, "익산": 38, "군산": 39, "목포": 40, "여수": 41, "순천": 42,
    # 대전·충청
    "대전": 43, "세종": 43,
    "청주": 46, "천안": 47, "충주": 48, "보령": 49, "서산": 50,
    # 울산·경남
    "울산": 51,
    "창원": 52, "양산": 53, "김해": 54, "진주": 55, "통영": 56,
    # 강원
    "강원": 57, "원주": 57,
    "춘천": 58, "강릉": 59, "태백": 60, "영월": 61,
    # 제주
    "제주": 62,
    # 도 이름 → 지역본부
    "경기": 9,   # 경인지역본부
    "충북": 46,  # 청주지사
    "충남": 47,  # 천안지사
    "전북": 37,  # 전주지사
    "전남": 40,  # 목포지사
    "경북": 27,  # 대구지역본부
    "경남": 52,  # 창원지사
}
```

**매핑 규칙**:
- 광역시·도 이름 → 해당 지역 대표 지사/지역본부
- 구·군 이름 → 해당 관할 지사
- "~구" 접미사 있는 것과 없는 것 모두 등록 (서울 구 단위)
- 시 이름 → 해당 지사 (경기 시 단위)
- "경기광주" → 성남지사 (광주광역시와 구분)

#### 1d. 조회 함수

```python
_METRO_KEYWORDS = frozenset([
    "서울", "부산", "대구", "인천", "광주", "대전", "세종", "울산",
    "경기", "강원", "충북", "충남", "전북", "전남", "경북", "경남", "제주",
])


def find_office(query: str) -> ComwelOffice | None:
    """질문 텍스트에서 지역명을 추출하여 관할 근로복지공단 지사 반환.

    구·군·시 단위 키워드를 광역시·도 이름보다 우선 매칭합니다.
    """
    metro_match = None
    for keyword, idx in REGION_MAP.items():
        if keyword in query:
            if keyword not in _METRO_KEYWORDS:
                return OFFICES[idx]
            elif metro_match is None:
                metro_match = idx
    if metro_match is not None:
        return OFFICES[metro_match]
    return None


def format_office(office: ComwelOffice) -> str:
    """근로복지공단 지사 정보를 답변 삽입용 텍스트로 포맷."""
    lines = [
        f"📍 근로복지공단 {office.name}",
        f"   전화: {office.phone}",
        f"   대표전화: 1588-0075",
        f"   주소: {office.address}",
        f"   관할: {', '.join(office.jurisdiction)}",
    ]
    if office.parent:
        lines.append(f"   (상위: {office.parent})")
    return "\n".join(lines)


def format_office_guide() -> str:
    """근로복지공단 검색 안내 (지역 미명시 시 사용 — 63개 전체 나열 대신)."""
    return (
        "관할 근로복지공단 지사를 찾으시려면:\n"
        "- 근로복지공단 고객센터: 1588-0075 (평일 09:00~18:00)\n"
        "- 지사 찾기: https://www.comwel.or.kr/comwel/intr/srch/srch.jsp\n"
        "- 지역명을 알려주시면 관할 지사를 안내해 드립니다."
    )
```

**설계 포인트**:
- `find_office()`: employment_centers.py의 `find_center()`와 동일한 `_METRO_KEYWORDS` 우선순위 패턴 적용
- `format_office()`: 직통전화 + 대표전화(1588-0075) 모두 표시
- `format_office_guide()`: 63개소이므로 전체 나열 대신 검색 안내 (employment_centers와 동일 전략)

---

### D-2. `app/core/pipeline.py` — 시스템 프롬프트 업데이트

#### 2a. SYSTEM_PROMPT에 규칙 추가 (기존 12번 뒤에)

```python
# 기존 12. 고용센터 연락처... 뒤에 추가:

"""
13. **근로복지공단 연락처가 포함된 경우**:
   - 산재보험(요양·휴업·장해급여), 체당금(체불임금보장), 근로복지대부(생활·주거안정자금),
     퇴직연금, 직업재활 등 근로복지공단 소관 사안이면 제공된 근로복지공단 연락처를 답변에 포함하세요.
   - 근로복지공단 대표전화 1588-0075도 함께 안내하세요.
   - 지역 정보가 없으면 1588-0075 + 지사 찾기 링크를 안내하세요."""
```

---

### D-3. `app/core/pipeline.py` — 컨텍스트에 근로복지공단 정보 삽입

#### 3a. import 추가

```python
from app.core.comwel_offices import find_office, format_office, format_office_guide
```

#### 3b. 컨텍스트 구성 구간 (기존 고용센터 블록 뒤, `parts.append(f"질문: {query}")` 전에)

```python
# 기존 고용센터 블록 뒤에 추가:

# 근로복지공단 연락처 (산재·체당금·복지대부 관련 시)
_COMWEL_KEYWORDS = ["산재보험", "산업재해", "산재신청", "산재",
                    "요양급여", "휴업급여", "장해급여", "유족급여",
                    "장의비", "간병급여",
                    "체당금", "체불임금보장",
                    "생활안정자금", "주거안정자금", "근로복지대부",
                    "퇴직연금", "직업재활", "근로자건강센터", "근로복지공단"]
if any(kw in query for kw in _COMWEL_KEYWORDS):
    office = find_office(query)
    if office:
        parts.append(f"관할 근로복지공단 연락처:\n\n{format_office(office)}")
    else:
        parts.append(f"근로복지공단 안내:\n\n{format_office_guide()}")

parts.append(f"질문: {query}")
```

**핵심 로직**: 질문에 산재/체당금/복지대부 등 키워드가 있으면 근로복지공단 정보를 Claude 컨텍스트에 삽입. 지역명이 있으면 관할 1개소, 없으면 검색 안내.

**키워드 충돌 방지**: `_COMMISSION_KEYWORDS`(노동위원회) ∩ `_CENTER_KEYWORDS`(고용센터) ∩ `_COMWEL_KEYWORDS`(근로복지공단) = ∅. 세 블록 모두 독립적으로 평가되므로, "산재 후 해고 + 실업급여" 같은 복합 질문에서는 3개 기관 연락처 모두 삽입 가능.

---

### D-4. `wage_calculator/legal_hints.py` — 면책 문구 보완

#### 4a. 기존 면책 문구 (line 260~265)

```python
# Before (현재)
lines.append(
    "\n⚠️ 위 내용은 참고용이며 법적 판단이 아닙니다. "
    "정확한 판단은 고용노동부(1350) 또는 노무사에게 문의하세요. "
    "부당해고·차별시정 구제신청은 관할 지방노동위원회에, "
    "실업급여·직업훈련은 관할 고용센터에 접수합니다."
)

# After
lines.append(
    "\n⚠️ 위 내용은 참고용이며 법적 판단이 아닙니다. "
    "정확한 판단은 고용노동부(1350) 또는 노무사에게 문의하세요. "
    "부당해고·차별시정 구제신청은 관할 지방노동위원회에, "
    "실업급여·직업훈련은 관할 고용센터에, "
    "산재보험·체당금은 관할 근로복지공단(1588-0075)에 접수합니다."
)
```

변경 범위 최소화: 기존 문장을 확장하여 근로복지공단도 언급. 상세 연락처는 D-3에서 pipeline이 제공.

---

## 3. 구현 순서

```
Step 1: app/core/comwel_offices.py — ComwelOffice 데이터 + REGION_MAP + 조회 함수
Step 2: app/core/pipeline.py — import + SYSTEM_PROMPT 규칙 13 추가 + 컨텍스트 삽입
Step 3: wage_calculator/legal_hints.py — 면책 문구 확장
Step 4: 통합 테스트 — 키워드·지역 조합별 동작 확인
```

---

## 4. 변경하지 않는 것

| 항목 | 이유 |
|------|------|
| `app/core/labor_offices.py` | 노동위원회 전용 모듈 — 근로복지공단과는 별개 기관 |
| `app/core/employment_centers.py` | 고용센터 전용 모듈 — 근로복지공단과는 별개 기관 |
| `harassment_assessor/` | 괴롭힘은 고용노동부 소관 — 근로복지공단 아님 |
| `chatbot.py` (CLI) | API 파이프라인에만 통합, CLI는 개발용 |
| `_extract_params()` | 근로복지공단은 계산기 파라미터가 아님 |
| `WageInput` / 계산기 모듈 | 연락처 기능은 계산과 무관 |

---

## 5. 검증 매트릭스

| 검증 항목 | 입력 조건 | 기대 결과 |
|----------|----------|----------|
| 지역 + 산재 | "서울 강남에서 산재보험 신청하려면?" | 서울강남지사 (02-3459-7153) |
| 지역 + 체당금 | "부산에서 체당금 신청" | 부산지역본부 (051-661-0187) |
| 경기 시 단위 | "수원에서 생활안정자금 대출" | 수원지사 (031-231-4204) |
| 광역 매칭 | "대전에서 산재 요양급여" | 대전지역본부 (042-870-9163) |
| 지역 미명시 | "산재보험 신청 방법" | 1588-0075 + 지사 찾기 링크 (전체 목록 아님) |
| 비관련 질문 | "주휴수당 계산해주세요" | 근로복지공단 정보 미삽입 (기존대로) |
| 노동위원회 소관 | "부당해고 구제신청" | 노동위원회 안내 유지 — 근로복지공단 아님 |
| 고용센터 소관 | "실업급여 신청하려면?" | 고용센터 안내 유지 — 근로복지공단 아님 |
| 복합 질문 | "산재 후 해고당해서 실업급여도 받고 싶어요" | 근로복지공단 + 노동위원회 + 고용센터 모두 삽입 |
| 대표전화 포함 | 근로복지공단 안내 시 | 1588-0075 대표전화 포함 |
| find_office 정확도 | 모든 REGION_MAP 키워드 | 올바른 지사 반환 |
| 키워드 3중 충돌 없음 | _COMMISSION ∩ _CENTER ∩ _COMWEL = ∅ | 세 세트 완전 분리 |
